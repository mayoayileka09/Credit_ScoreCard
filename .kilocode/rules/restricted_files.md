# Restricted Files

## MUST NOT READ, WRITE, OR COMMIT

- `.env`, `.env.*`
- `.venv`, `.venv.*`
- `supersecrets.txt`
- `credentials.json`
- `*.pem`, `*.key`
- `id_rsa`, `id_rsa.pub`
- Any file in `data/` containing PHI/PII